import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  avatar: {
    height: 50,
    width: 50,
  },
  container: {
    display: "flex",
    flexDirection: "row",
  },
  leftContainer: {
    flexDirection: "row",
  },
  rightContainer: {},
  midContainer: {},
});

export default styles;
